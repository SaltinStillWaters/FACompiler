to do (not immediate):

resolve sheet names being too long

extract info from canvas and write to sheets

way to stop extension when accessing SAs

decide whether to keep question id or not

======================================================

to do (next):

finish currSheet class
in checking if a question exists, check length
encapsulate global vars
make it so that all methods does not use global variables. Instead all global variables must be in main.js







